import React from 'react';

const styles = {
    container: {
        display: 'flex',
    },
};

export default class Xxxx extends React.Component {
    render() {
        return (
            <div style={styles.container}>
            </div>
        );
    }
}
